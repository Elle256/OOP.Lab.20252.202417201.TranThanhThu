Question: If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this?

No, Java does not allow this. Because both constructors would have the same parameter type (String), Java cannot distinguish between them. Constructor overloading requires different parameter types or a different number of parameters, not just different parameter names.